import { Link } from "react-router-dom";

const Offer = () => {
    return (
        <div>
            <p>Welcome to Offer </p>
            <Link to="/"> Go to Home</Link>;
        </div>
    );
};

export default Offer;
